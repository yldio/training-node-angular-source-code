{
  "name": "toodoo-app",
  "version": "0.0.1",
  "description": "Your average todo application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-static": "*",
    "nano": "*",
    "restify": "*",
    "bcrypt": "*"
  }
}
